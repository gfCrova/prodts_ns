import styles from './All.module.css'

const Logo = () => {
        return (
            <div className={`${styles.logo}`}>
                Shop
            </div>
        );
    }

export default Logo;
